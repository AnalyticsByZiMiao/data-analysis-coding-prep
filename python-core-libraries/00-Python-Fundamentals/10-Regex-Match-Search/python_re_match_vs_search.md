# RE 模块中的 match() 与 search() 

```

| 特性 | match() | search() |
|------|---------|----------|
|: 匹配起始位置 :|: 仅字符串开头 :|: 字符串任意位置 :|
| 成功匹配返回值 | 匹配对象（Match object） | 匹配对象（Match object） |
| 匹配失败返回值 | None | None |
| 典型应用场景 | 验证字符串开头（如检查文件头、特定前缀） | 在字符串中查找任意位置的模式（如提取数据） |

```
